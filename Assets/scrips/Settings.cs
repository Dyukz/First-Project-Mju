using UnityEngine;

public class Settings : MonoBehaviour
{   
    
    public KeyCode set_forward = KeyCode.W;
    public KeyCode set_left = KeyCode.A;
    public KeyCode set_backwards = KeyCode.S;
    public KeyCode set_right = KeyCode.D;
    public KeyCode set_jump = KeyCode.Space;
    public KeyCode set_sprint = KeyCode.LeftShift;
    public KeyCode set_interact = KeyCode.E;



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
